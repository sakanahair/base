rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all users (temporary for testing)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}