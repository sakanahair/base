rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 開発環境用: 認証されたユーザーは全てのドキュメントにアクセス可能
    // 本番環境では必ず適切なルールに変更してください
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}